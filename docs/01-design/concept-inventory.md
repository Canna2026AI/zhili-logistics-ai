# 视觉概念清单与实现约束

这些图片是实施规格，不是最终 UI 图片资源。导航、按钮、表格、表单、文字和状态必须使用真实 HTML/React 组件实现，禁止将概念图直接作为页面背景。

| 文件 | 原生画布 | 核心验证点 |
| --- | --- | --- |
| `concepts/00-ops-waybill-list.png` | 1586×992 | 统一运营壳、页签、状态计数、密集表格、标准运单和右侧详情抽屉 |
| `concepts/01-order-quote.png` | 1585×992 | 分区表单、标准报价、费用解释、单一主命令和权限化更多操作 |
| `concepts/02-warehouse-workbench.png` | 1586×992 | 扫码、实物差异、图片、设备、报价限制和一屏确认 |
| `concepts/03-finance-ar.png` | 1586×992 | 标准费用、状态计数、版本差异、利润影响、守恒和审核轨迹 |
| `concepts/04-customer-dashboard.png` | 1586×992 | 客户任务、标准运单、预存款、支付订单、账单、工单和通知 |
| `concepts/05-platform-console.png` | 1586×992 | 租户、尾程/支付模块、配额、健康、限时代入和审计 |
| `concepts/06-pda-receive-offline.png` | 1672×941 | 双 390×844 设备参考：离线收货、媒体补传、队列、过期登录和三类冲突处理 |
| `concepts/07-marketing-home.png` | 1561×1008 | 纯色官网首屏、统一 AppShell 产品预览、尾程/POD、结算、安全部署和 2026 法律页脚 |

## 不可偏离的共同约束

- 主工作区必须为 `#FFFFFF`，页面底色为 `#F8FAFC`；不得改为米白或奶油色。
- 主导航为 `#1F2937`，主操作为 `#0F766E`；不得增加装饰性渐变和霓虹光。
- 桌面组件以 4–6px 圆角为主；PDA 可使用 6–8px，不得变成大圆角卡片系统。
- 数据表保留清晰表头、行分隔、状态、选择和行操作，不得改造成卡片网格。
- 详情抽屉与主表同时可见，关闭后恢复原筛选、滚动和选择状态。
- 所有概念中文字作为领域语义参考；编号、重量、金额和公式必须与 `canonical-fixtures.md` 一致，图片仍不得反向覆盖可执行 fixture。
- 图标统一使用 1.75–2px 圆角线性 SVG，状态图标可填充；不使用文本箭头替代。

## 允许的响应式调整

- 运营端低于 1180px 时允许横向滚动和折叠导航，不把财务/运单宽表压成卡片。
- 客户门户在移动端可把右栏模块移到主表下方，核心快捷操作保持首屏可见。
- PDA 只保留任务相关信息，不能复制桌面导航或宽表。
- 官网在移动端按“文案—操作—产品预览—流程—能力—安全—CTA”顺序单列。

## 浏览器视觉验收

每个主界面在对应原生画布或同等比例截图，至少比较：

1. 导航宽度、顶栏、页签和首屏信息密度。
2. 字体家族、字号、行高和数字对齐。
3. 白色、石墨、青绿、边界和状态色。
4. 表单/表格/抽屉的容器模型、间距和圆角。
5. 选中、悬停、焦点、加载、错误、权限和数据过期状态。
6. PDA 触控尺寸、离线反馈和冲突操作。
7. 官网首屏文案、按钮、产品预览与下一段可见比例。

任何一项仍会收到设计评审修改意见时，不得标记视觉完成。
