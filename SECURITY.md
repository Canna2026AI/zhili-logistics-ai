# 安全策略

## 报告漏洞

请不要在公开 Issue 中披露未修复漏洞。通过 GitHub 仓库的 Security Advisory 私密报告，并提供影响范围、复现步骤、预期结果和建议修复。维护者会在确认后协调修复与披露。

## 安全边界

- 所有租户业务数据同时经过应用权限与 PostgreSQL RLS，默认拒绝跨租户访问。
- 密码使用 Argon2id；刷新会话轮换并使用 HttpOnly Cookie。
- 写命令使用幂等键；聚合并发修改使用 `If-Match`；关键行为写不可变审计。
- API、连接器、支付与 Webhook 密钥不得进入仓库或日志；示例只使用无效演示值。
- 已审核财务记录只能反审核、冲销或调整；支付回调和分配必须满足金额守恒。
- AI 自动化继承调用者权限；关键财务、扣放货、删除和敏感导出必须人工确认。

## 支持版本

安全修复只保证当前 `main` 与最新稳定发布。部署方应及时更新依赖和镜像，并定期执行备份恢复演练。
